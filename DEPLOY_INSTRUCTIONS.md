# 📋 Инструкция по деплою админ-панели

## ✅ Что уже готово

- ✅ Отдельный проект `delever-admin` создан
- ✅ Визуальный редактор уровня Webflow
- ✅ API интеграция для синхронизации
- ✅ Конфигурация для Vercel
- ✅ Все файлы закоммичены

## 🚀 Шаги для деплоя

### 1. Создайте репозиторий на GitHub

```bash
# Перейдите на https://github.com/new
# Название: delever-admin
# Видимость: Private (рекомендуется)
# НЕ добавляйте файлы при создании
```

### 2. Подключите к GitHub

```bash
cd delever-admin

# Добавьте remote (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/delever-admin.git

# Отправьте код
git push -u origin main
```

### 3. Деплой на Vercel

**Через веб-интерфейс:**

1. Зайдите на https://vercel.com
2. Нажмите "Add New Project"
3. Импортируйте репозиторий `delever-admin`
4. Настройки:
   - **Framework Preset:** Vite
   - **Root Directory:** `./`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`
5. Добавьте переменные окружения:
   ```
   VITE_SITE_URL=https://delever.io
   VITE_API_URL=https://api.delever.io
   ```
6. Нажмите "Deploy"

### 4. Настройте домен

1. В настройках проекта Vercel → **Domains**
2. Добавьте: `admin.delever.io`
3. Настройте DNS у регистратора домена:
   - **Тип:** CNAME
   - **Имя:** `admin`
   - **Значение:** значение, указанное Vercel

### 5. Проверка

Откройте `https://admin.delever.io` и войдите:
- Email: `admin@delever.io`
- Пароль: `admin123`

## 🔗 Подключение к основному сайту

Админка подключается к основному сайту через:

1. **Iframe** - для предпросмотра страниц
2. **API** - для синхронизации контента

### Настройка API на основном сайте

Создайте API endpoints (можно через Vercel Serverless Functions):

```
GET  /api/content        - Получить контент
POST /api/content/sync   - Синхронизировать контент
POST /api/content/publish - Опубликовать изменения
```

Или используйте готовое решение:
- Firebase Realtime Database
- Supabase
- Strapi
- Contentful

## 📝 Текущее состояние

- ✅ Проект готов к деплою
- ✅ Все файлы закоммичены
- ⏳ Нужно создать GitHub репозиторий
- ⏳ Нужно задеплоить на Vercel
- ⏳ Нужно настроить API endpoints (опционально)

## 🎯 Следующие шаги

1. Создайте репозиторий на GitHub
2. Отправьте код: `git push -u origin main`
3. Задеплойте на Vercel
4. Настройте домен `admin.delever.io`
5. (Опционально) Настройте API для синхронизации

## 💡 Советы

- Используйте Private репозиторий для безопасности
- Настройте переменные окружения в Vercel
- Измените пароль по умолчанию перед продакшеном
- Настройте CORS для API endpoints

